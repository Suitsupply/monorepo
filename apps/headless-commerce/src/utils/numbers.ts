/**
 * Converts a whole number percentage to its decimal representation.
 * @param percentage - The whole number percentage to convert.
 * @returns The decimal representation of the percentage.
 */
export function getDecimalFromWholeNumber(percentage: number): number {
  return percentage / 100;
}
